// Q-1 What is the use of axios?

// A) auto converts data to json
// B) used to handle interceptors
// C) used to handle transformers
// D) all the above

// Res: D) all the above
// Reason: Axios used to handle transformers & interceptors & auto converts data to json.

// Q-2 Which is used to redirect to another page without page refresh?

// A) <a>
// B) <Link>
// C) both 1 and 2
// D) none

// Res: B) <Link>
// Reason: Because anchor tag will redirect with page reload/refresh but Link will not reload/refresh to navigate to another page

// Q-3 Which hook is used to get parameters from URL?

// A) useParam
// B) useLocation
// C) useHistory
// D) none

// Res: A) useParam
// Reason: In useParam() hook, we use /: colon to add parameters and separate each parameter.

// Q-4 What will happen if you render an input element with disabled = {false}
    
// A) It will be rendered as disabled
// B) It will not be rendered at all
// C) It will be rendered as enabled
// D) none

// Res: C) It will be rendered as enabled
// Reason: As we given false to get from disabled, so we rendered as enabled.

// Q-5 What will uglify plugin do?
    
// A) enables uglify in the webpack-config
// B) enables uglify in browser DOM
// C) both 1 and 2
// D) none

// Res: A) enables uglify in the webpack-config
// Reason: "uglify" is a plugin in React which enables uglify in the webpack-config

// Q-6 Redirect is used for?

// A) Switch across the Route
// B) Navigate to page based on validation
// C) link the page
// D) none

// Res: B) Navigate to page based on validation
// Reason: Redirect is used for navigation from one page to another based on validation (true/false).

// Q-7 Does react-router and react-router-dom provide same methods?
// A) Yes
// B) No

// Res: B) No
// Reason: Because both are npm packages of router but built-in methods are entirely different.

// Q-8 Which are the below React-router-dom methods
// A) BrowserRouter
// B) Switch
// C) IndexedRoute
// D) NavLink

// Res: A) BrowserRouter & B) Switch
// Reason: Only Switch & BrowserRouter are available methods of list above. We used BrowserRouter to wrap <App /> at index.js file.

// Q-9 Different ways to get params?
    
// A) props.match.params
// B) useParams
// C) props.location.params 
// D) none

// Res: A) props.match.params & B) useParams
// Reason: To get params, we use useParams Hook & traditional way using props.attributeName.params

// Q-10: On page refresh will the data sent via Link get erased?
    
// A) Yes
// B) No

// Res: A) Yes
// Reason: On page refresh, the data sent via Link will get erased always. It will erase.